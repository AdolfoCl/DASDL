// This file was generated by Trackway C++ 1.0.8.
// Timestamp: 2026-08-29 12:41:16
// Regenerate to apply syntax changes; manual edits will be overwritten.

#include "../properties/properties.hpp"
#include <cassert>
#include <fstream>
#include <iostream>
#include <memory>
#include <string>
#include <vector>

using std::cerr;
using std::cout;
using std::endl;

int MINESCAPE = 0;
std::vector<int> ESCSTACK(1024, 0);
int IESCSTACK = -1;
// Token type codes live in this generated workfile.
extern const unsigned char BADTOK = 0x01;
extern const unsigned char IDNTOK = 0x02;
extern const unsigned char RESTOK = 0x03; // DEFAULT TOKEN TYPE
extern const unsigned char NUMTOK = 0x04;
extern const unsigned char INTTOK = 0x05;
extern const unsigned char STRTOK = 0x06;
extern const unsigned char CHRTOK = 0x07; // CHR TOKEN TYPE
extern const unsigned char TXTTOK = 0x08;
extern const unsigned char ETXTOK = 0x09;
// Token has no default constructor in this legacy properties implementation.
Token token(BADTOK, "");
// IMPORTANT:
// Do NOT default-construct Parser as a global, because Parser::Parser() reads
// from stdin. That would block before main() runs. Construct it inside main()
// instead.
std::unique_ptr<Parser> parser;

// Initialize reserved words list
// Note: Values are stored without any trailing spaces.
std::vector<std::string> Parser::reservedWords = {
      "IS",
      "NO",
      "OC",
      "OF",
      "ON",
      "OR",
      "TO",
      "ALL",
      "AND",
      "BIT",
      "EQL",
      "GEQ",
      "GTR",
      "I-R",
      "I-S",
      "KEY",
      "LEQ",
      "LSS",
      "NEQ",
      "NOT",
      "O-L",
      "PER",
      "SET",
      "BITS",
      "DATA",
      "FILE",
      "LAST",
      "LIST",
      "NONE",
      "NULL",
      "REAL",
      "SETS",
      "SIZE",
      "TRUE",
      "USER",
      "ALPHA",
      "AUDIT",
      "BYTES",
      "COUNT",
      "FALSE",
      "FIELD",
      "FIRST",
      "GROUP",
      "INDEX",
      "MODEL",
      "TIMES",
      "TRAIL",
      "WHERE",
      "WORDS",
      "ACCESS",
      "BLANKS",
      "BLOCKS",
      "DIRECT",
      "FILLER",
      "NUMBER",
      "OCCURS",
      "RANDOM",
      "REMAPS",
      "SELECT",
      "SERIAL",
      "SUBSET",
      "UPDATE",
      "VECTOR",
      "VERIFY",
      "BOOLEAN",
      "COMPACT",
      "CONTROL",
      "DATASET",
      "ENTRIES",
      "OPTIONS",
      "ORDERED",
      "RECORDS",
      "RESTART",
      "SECONDS",
      "VIRTUAL",
      "DATA-SET",
      "DATABASE",
      "DEFAULTS",
      "READONLY",
      "RECOVERY",
      "REQUIRED",
      "SEGMENTS",
      "STANDARD",
      "ASCENDING",
      "DATA-BASE",
      "DEPENDING",
      "DMCONTROL",
      "DMSUPPORT",
      "DMUTILITY",
      "GUARDFILE",
      "LOW-VALUE",
      "UNORDERED",
      "DESCENDING",
      "DUPLICATES",
      "HIGH-VALUE",
      "LOBUTILITY",
      "PARAMETERS",
      "POPULATION",
      "SEQUENTIAL",
      "SYNCPOINTS",
      "KEYCHANGEOK",
      "RECONSTRUCT",
      "DATARECOVERY",
      "INITIALVALUE",
      "TRANSACTIONS",
      "ACCESSROUTINES",
      "REORGANIZATION",
};

int getRoot();
int getDatabaseElement();
int getModelOption();
int getUpdateOption();
int getOptionsSpecification();
int getParametersSpecification();
int getDefaultsSpecification();
int getCodeFileSpecification();
int getControlFileSpecification();
int getAuditTrailSpecification();
int getStructureDeclaration();
int getCodeFileName();
int getFileTitle();
int getUsercode();
int getFileName();
int getOnFamily();
int getPhysicalOptions();
int getDefaultEntryList();
int getDefaultEntry();
int getStructureDefault();
int getAttribute();
int getStructureDefaultKind();
int getDataSetWord();
int getStructureBody();
int getSetDeclaration();
int getSubsetDeclaration();
int getAccessDeclaration();
int getRemapDeclaration();
int getLogicalDatabaseDeclaration();
int getVirtualDeclaration();
int getItemDescription();
int getItemAttributeList();
int getDataSetDeclaration();
int getDataSetKind();
int getRecordDescription();
int getStructureTail();
int getAttributeList();
int getRecordEntryList();
int getRecordEntry();
int getRename();
int getAlphaItem();
int getNumberItem();
int getBooleanItem();
int getRealItem();
int getFieldItem();
int getGroupItem();
int getPopulationItem();
int getCountItem();
int getNumericSize();
int getScaleFactor();
int getFieldBody();
int getOfStructure();
int getItemAttribute();
int getIsOrEquals();
int getItemValue();
int getOccursClause();
int getDependingOn();
int getWhereCondition();
int getSetType();
int getKeyClause();
int getKeySpec();
int getSetTail();
int getSetOptionList();
int getSetOption();
int getDuplicatesOption();
int getKeyItemList();
int getKeyItem();
int getKeyOrder();
int getAttributeValue();
int getValueExpression();
int getNumericValue();
int getNumericQualifierList();
int getNumericQualifier();
int getUnit();
int getPerClause();
int getBufferKind();
int getBooleanExpression();
int getBooleanTerm();
int getBooleanTail();
int getNegation();
int getBooleanPrimary();
int getComparison();
int getOperand();
int getComparisonTail();
int getRelationalOperator();
int getOperandPrimary();
int getArithmeticTail();
int getArithmeticOperator();
int getRemapRecordDescription();
int getVariableFormatPart();
int getSelectVerifyList();
int getVirtualValue();
int getVariableFormatRecord();
int getSelectVerify();
int getDatabaseWord();
int getStructureList();
int getLogicalDatabaseOption();
int getStructureListEntry();
int getSetPart();
int getSetList();
int getSetListEntry();

static int ESCAPE_PROCEDURE(std::vector<int> &escstack, int &iescstack) {
  if (iescstack < 0) {
    return 0;
  }
  const int initial_iescstack = iescstack;
  while (token.type != ETXTOK) {
    while (iescstack >= 0) {
      const int inx = escstack[static_cast<size_t>(iescstack)];
      switch (inx) {
      case 1:
        if (token == Token(RESTOK, "MODEL")) return inx;
        break;
      case 2:
        if (token == Token(RESTOK, "UPDATE")) return inx;
        break;
      case 3:
        if (token == Token(RESTOK, "OPTIONS")) return inx;
        break;
      case 4:
        if (token == Token(RESTOK, "PARAMETERS")) return inx;
        break;
      case 5:
        if (token == Token(RESTOK, "DEFAULTS")) return inx;
        break;
      case 6:
        if (token == Token(RESTOK, "ACCESSROUTINES")) return inx;
        break;
      case 7:
        if (token == Token(RESTOK, "DMCONTROL")) return inx;
        break;
      case 8:
        if (token == Token(RESTOK, "DMUTILITY")) return inx;
        break;
      case 9:
        if (token == Token(RESTOK, "DMSUPPORT")) return inx;
        break;
      case 10:
        if (token == Token(RESTOK, "LOBUTILITY")) return inx;
        break;
      case 11:
        if (token == Token(RESTOK, "RECOVERY")) return inx;
        break;
      case 12:
        if (token == Token(RESTOK, "DATARECOVERY")) return inx;
        break;
      case 13:
        if (token == Token(RESTOK, "RECONSTRUCT")) return inx;
        break;
      case 14:
        if (token == Token(RESTOK, "REORGANIZATION")) return inx;
        break;
      case 15:
        if (token == Token(RESTOK, "CONTROL")) return inx;
        break;
      case 16:
        if (token == Token(RESTOK, "AUDIT")) return inx;
        break;
      case 17:
        if (token == Token(RESTOK, "SET")) return inx;
        break;
      case 18:
        if (token == Token(RESTOK, "SUBSET")) return inx;
        break;
      case 19:
        if (token == Token(RESTOK, "ACCESS")) return inx;
        break;
      case 20:
        if (token == Token(RESTOK, "REMAPS")) return inx;
        break;
      case 21:
        if (token == Token(RESTOK, "DATABASE")) return inx;
        break;
      case 22:
        if (token == Token(RESTOK, "DATA-BASE")) return inx;
        break;
      case 23:
        if (token == Token(RESTOK, "VIRTUAL")) return inx;
        break;
      case 24:
        if (token == Token(RESTOK, "OF")) return inx;
        break;
      case 25:
        if (token == Token(CHRTOK, "(")) return inx;
        break;
      case 26:
        if (token == Token(RESTOK, "ALPHA")) return inx;
        break;
      case 27:
        if (token == Token(RESTOK, "NUMBER")) return inx;
        break;
      case 28:
        if (token == Token(RESTOK, "BOOLEAN")) return inx;
        break;
      case 29:
        if (token == Token(RESTOK, "REAL")) return inx;
        break;
      case 30:
        if (token == Token(RESTOK, "FIELD")) return inx;
        break;
      case 31:
        if (token == Token(RESTOK, "GROUP")) return inx;
        break;
      case 32:
        if (token == Token(RESTOK, "POPULATION")) return inx;
        break;
      case 33:
        if (token == Token(RESTOK, "COUNT")) return inx;
        break;
      case 34:
        if (token == Token(RESTOK, "COMPACT")) return inx;
        break;
      case 35:
        if (token == Token(RESTOK, "DIRECT")) return inx;
        break;
      case 36:
        if (token == Token(RESTOK, "ORDERED")) return inx;
        break;
      case 37:
        if (token == Token(RESTOK, "RANDOM")) return inx;
        break;
      case 38:
        if (token == Token(RESTOK, "RESTART")) return inx;
        break;
      case 39:
        if (token == Token(RESTOK, "STANDARD")) return inx;
        break;
      case 40:
        if (token == Token(RESTOK, "UNORDERED")) return inx;
        break;
      case 41:
        if (token == Token(RESTOK, "REQUIRED")) return inx;
        break;
      case 42:
        if (token == Token(RESTOK, "READONLY")) return inx;
        break;
      case 43:
        if (token == Token(RESTOK, "NULL")) return inx;
        break;
      case 44:
        if (token == Token(RESTOK, "INITIALVALUE")) return inx;
        break;
      case 45:
        if (token == Token(RESTOK, "OCCURS")) return inx;
        break;
      case 46:
        if (token == Token(RESTOK, "OC")) return inx;
        break;
      case 47:
        if (token == Token(CHRTOK, ",")) return inx;
        break;
      case 48:
        if (token == Token(RESTOK, "FILLER")) return inx;
        break;
      case 49:
        if (token == Token(CHRTOK, ",")) return inx;
        break;
      case 50:
        if (token == Token(CHRTOK, ",")) return inx;
        break;
      default:
        break;
      }
      iescstack -= 1;
    }
    token = parser->nextToken();
    iescstack = initial_iescstack;
  }
  return 0;
}

static constexpr int BADENTITY = 1;
static constexpr int ISNTENTITY = 2;

int getRoot() {
  bool entityEntered = true;
  int INITIAL_IESCSTACK = 0;
  int INXESCAPELABEL = 0;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto LABA4;
LABA2B1:
  INITIAL_IESCSTACK = IESCSTACK;
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 1;  //--> RESTOK "MODEL"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 2;  //--> RESTOK "UPDATE"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 3;  //--> RESTOK "OPTIONS"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 4;  //--> RESTOK "PARAMETERS"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 5;  //--> RESTOK "DEFAULTS"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 6;  //--> RESTOK "ACCESSROUTINES"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 7;  //--> RESTOK "DMCONTROL"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 8;  //--> RESTOK "DMUTILITY"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 9;  //--> RESTOK "DMSUPPORT"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 10;  //--> RESTOK "LOBUTILITY"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 11;  //--> RESTOK "RECOVERY"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 12;  //--> RESTOK "DATARECOVERY"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 13;  //--> RESTOK "RECONSTRUCT"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 14;  //--> RESTOK "REORGANIZATION"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 15;  //--> RESTOK "CONTROL"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 16;  //--> RESTOK "AUDIT"
  if ((INVOKERESULT = getDatabaseElement()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      IESCSTACK = INITIAL_IESCSTACK;
      goto LABA2A1;
    }
    INXESCAPELABEL = ESCAPE_PROCEDURE(ESCSTACK, IESCSTACK);
    if (IESCSTACK <= INITIAL_IESCSTACK) return BADENTITY;
    IESCSTACK = INITIAL_IESCSTACK;
    switch (INXESCAPELABEL) {
      case 1: //--> RESTOK "MODEL"
      case 2: //--> RESTOK "UPDATE"
      case 3: //--> RESTOK "OPTIONS"
      case 4: //--> RESTOK "PARAMETERS"
      case 5: //--> RESTOK "DEFAULTS"
      case 6: //--> RESTOK "ACCESSROUTINES"
      case 7: //--> RESTOK "DMCONTROL"
      case 8: //--> RESTOK "DMUTILITY"
      case 9: //--> RESTOK "DMSUPPORT"
      case 10: //--> RESTOK "LOBUTILITY"
      case 11: //--> RESTOK "RECOVERY"
      case 12: //--> RESTOK "DATARECOVERY"
      case 13: //--> RESTOK "RECONSTRUCT"
      case 14: //--> RESTOK "REORGANIZATION"
      case 15: //--> RESTOK "CONTROL"
      case 16: goto LABA2B1; //--> RESTOK "AUDIT"
    }
    return BADENTITY;
  } else {
    IESCSTACK = INITIAL_IESCSTACK;
  }
  if (!(token == Token(CHRTOK, ";")))
    goto LABA2A1;
  token = parser->nextToken();
  model.endDeclaration();
LABA3:
  goto LABA1;
LABA4:
  if (!(token.type == ETXTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("<END OF TEXT> EXPECTED");
    ENTITYRESULT = BADENTITY;
    INITIAL_IESCSTACK = IESCSTACK;
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 1;  //--> RESTOK "MODEL"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 2;  //--> RESTOK "UPDATE"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 3;  //--> RESTOK "OPTIONS"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 4;  //--> RESTOK "PARAMETERS"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 5;  //--> RESTOK "DEFAULTS"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 6;  //--> RESTOK "ACCESSROUTINES"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 7;  //--> RESTOK "DMCONTROL"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 8;  //--> RESTOK "DMUTILITY"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 9;  //--> RESTOK "DMSUPPORT"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 10;  //--> RESTOK "LOBUTILITY"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 11;  //--> RESTOK "RECOVERY"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 12;  //--> RESTOK "DATARECOVERY"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 13;  //--> RESTOK "RECONSTRUCT"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 14;  //--> RESTOK "REORGANIZATION"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 15;  //--> RESTOK "CONTROL"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 16;  //--> RESTOK "AUDIT"
    INXESCAPELABEL = ESCAPE_PROCEDURE(ESCSTACK, IESCSTACK);
    if (IESCSTACK <= INITIAL_IESCSTACK) return BADENTITY;
    IESCSTACK = INITIAL_IESCSTACK;
    switch (INXESCAPELABEL) {
      case 1: //--> RESTOK "MODEL"
      case 2: //--> RESTOK "UPDATE"
      case 3: //--> RESTOK "OPTIONS"
      case 4: //--> RESTOK "PARAMETERS"
      case 5: //--> RESTOK "DEFAULTS"
      case 6: //--> RESTOK "ACCESSROUTINES"
      case 7: //--> RESTOK "DMCONTROL"
      case 8: //--> RESTOK "DMUTILITY"
      case 9: //--> RESTOK "DMSUPPORT"
      case 10: //--> RESTOK "LOBUTILITY"
      case 11: //--> RESTOK "RECOVERY"
      case 12: //--> RESTOK "DATARECOVERY"
      case 13: //--> RESTOK "RECONSTRUCT"
      case 14: //--> RESTOK "REORGANIZATION"
      case 15: //--> RESTOK "CONTROL"
      case 16: goto LABA2B1; //--> RESTOK "AUDIT"
    }
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getRoot

int getDatabaseElement() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if ((INVOKERESULT = getModelOption()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1B1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1B1:
  if ((INVOKERESULT = getUpdateOption()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1C1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1C1:
  if ((INVOKERESULT = getOptionsSpecification()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1D1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1D1:
  if ((INVOKERESULT = getParametersSpecification()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1E1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1E1:
  if ((INVOKERESULT = getDefaultsSpecification()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1F1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1F1:
  if ((INVOKERESULT = getCodeFileSpecification()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1G1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1G1:
  if ((INVOKERESULT = getControlFileSpecification()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1H1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1H1:
  if ((INVOKERESULT = getAuditTrailSpecification()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1I1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1I1:
  if ((INVOKERESULT = getStructureDeclaration()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("<MODEL OPTION>");
      SEND("<UPDATE OPTION>");
      SEND("<OPTIONS SPECIFICATION>");
      SEND("<PARAMETERS SPECIFICATION>");
      SEND("<DEFAULTS SPECIFICATION>");
      SEND("<CODE FILE SPECIFICATION>");
      SEND("<CONTROL FILE SPECIFICATION>");
      SEND("<AUDIT TRAIL SPECIFICATION>");
      SEND("<STRUCTURE DECLARATION>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getDatabaseElement

int getModelOption() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "MODEL"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("MODEL EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
  if (!(token.type == IDNTOK)) {
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  model.setModelName(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getModelOption

int getUpdateOption() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "UPDATE"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("UPDATE EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.markUpdate();
  token = parser->nextToken();
LABA2:
  if (!(token.type == IDNTOK))
    goto EXIT;
  model.setUpdateName(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getUpdateOption

int getOptionsSpecification() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "OPTIONS"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("OPTIONS EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginOptions();
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, "("))) {
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if ((INVOKERESULT = getAttributeList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<ATTRIBUTE LIST> EXPECTED");
    }
    return BADENTITY;
  }
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getOptionsSpecification

int getParametersSpecification() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "PARAMETERS"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("PARAMETERS EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginParameters();
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, "("))) {
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if ((INVOKERESULT = getAttributeList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<ATTRIBUTE LIST> EXPECTED");
    }
    return BADENTITY;
  }
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getParametersSpecification

int getDefaultsSpecification() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "DEFAULTS"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("DEFAULTS EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginDefaults();
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, "("))) {
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if ((INVOKERESULT = getDefaultEntryList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<DEFAULT ENTRY LIST> EXPECTED");
    }
    return BADENTITY;
  }
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getDefaultsSpecification

int getCodeFileSpecification() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getCodeFileName()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<CODE FILE NAME> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
  if (!(token == Token(CHRTOK, "="))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("= EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
  if ((INVOKERESULT = getFileTitle()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<FILE TITLE> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getCodeFileSpecification

int getControlFileSpecification() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "CONTROL"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("CONTROL EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginControlFile();
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "FILE"))) {
    ERROR("FILE EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if ((INVOKERESULT = getPhysicalOptions()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<PHYSICAL OPTIONS> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getControlFileSpecification

int getAuditTrailSpecification() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "AUDIT"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("AUDIT EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginAuditTrail();
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "TRAIL"))) {
    ERROR("TRAIL EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if ((INVOKERESULT = getPhysicalOptions()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<PHYSICAL OPTIONS> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getAuditTrailSpecification

int getStructureDeclaration() {
  bool entityEntered = false;
  int INITIAL_IESCSTACK = 0;
  int INXESCAPELABEL = 0;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token.type == IDNTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("<IDENTIFIER> EXPECTED");
    ENTITYRESULT = BADENTITY;
    INITIAL_IESCSTACK = IESCSTACK;
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 17;  //--> RESTOK "SET"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 18;  //--> RESTOK "SUBSET"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 19;  //--> RESTOK "ACCESS"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 20;  //--> RESTOK "REMAPS"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 21;  //--> RESTOK "DATABASE"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 22;  //--> RESTOK "DATA-BASE"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 23;  //--> RESTOK "VIRTUAL"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 24;  //--> RESTOK "OF"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 25;  //--> CHRTOK "("
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 26;  //--> RESTOK "ALPHA"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 27;  //--> RESTOK "NUMBER"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 28;  //--> RESTOK "BOOLEAN"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 29;  //--> RESTOK "REAL"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 30;  //--> RESTOK "FIELD"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 31;  //--> RESTOK "GROUP"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 32;  //--> RESTOK "POPULATION"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 33;  //--> RESTOK "COUNT"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 34;  //--> RESTOK "COMPACT"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 35;  //--> RESTOK "DIRECT"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 36;  //--> RESTOK "ORDERED"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 37;  //--> RESTOK "RANDOM"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 38;  //--> RESTOK "RESTART"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 39;  //--> RESTOK "STANDARD"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 40;  //--> RESTOK "UNORDERED"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 41;  //--> RESTOK "REQUIRED"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 42;  //--> RESTOK "READONLY"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 43;  //--> RESTOK "NULL"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 44;  //--> RESTOK "INITIALVALUE"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 45;  //--> RESTOK "OCCURS"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 46;  //--> RESTOK "OC"
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 47;  //--> CHRTOK ","
    INXESCAPELABEL = ESCAPE_PROCEDURE(ESCSTACK, IESCSTACK);
    if (IESCSTACK <= INITIAL_IESCSTACK) return BADENTITY;
    IESCSTACK = INITIAL_IESCSTACK;
    switch (INXESCAPELABEL) {
      case 17: //--> RESTOK "SET"
      case 18: //--> RESTOK "SUBSET"
      case 19: //--> RESTOK "ACCESS"
      case 20: //--> RESTOK "REMAPS"
      case 21: //--> RESTOK "DATABASE"
      case 22: //--> RESTOK "DATA-BASE"
      case 23: //--> RESTOK "VIRTUAL"
      case 24: //--> RESTOK "OF"
      case 25: //--> CHRTOK "("
      case 26: //--> RESTOK "ALPHA"
      case 27: //--> RESTOK "NUMBER"
      case 28: //--> RESTOK "BOOLEAN"
      case 29: //--> RESTOK "REAL"
      case 30: //--> RESTOK "FIELD"
      case 31: //--> RESTOK "GROUP"
      case 32: //--> RESTOK "POPULATION"
      case 33: //--> RESTOK "COUNT"
      case 34: //--> RESTOK "COMPACT"
      case 35: //--> RESTOK "DIRECT"
      case 36: //--> RESTOK "ORDERED"
      case 37: //--> RESTOK "RANDOM"
      case 38: //--> RESTOK "RESTART"
      case 39: //--> RESTOK "STANDARD"
      case 40: //--> RESTOK "UNORDERED"
      case 41: //--> RESTOK "REQUIRED"
      case 42: //--> RESTOK "READONLY"
      case 43: //--> RESTOK "NULL"
      case 44: //--> RESTOK "INITIALVALUE"
      case 45: //--> RESTOK "OCCURS"
      case 46: //--> RESTOK "OC"
      case 47: goto LABA3; //--> CHRTOK ","
    }
    return BADENTITY;
  }
  entityEntered = true;
  model.beginEntry(token.value, token.line);
  token = parser->nextToken();
LABA2:
  if (!(token.type == STRTOK))
    goto LABA3;
  model.setComment(token.value);
  token = parser->nextToken();
LABA3:
  INITIAL_IESCSTACK = IESCSTACK;
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 17;  //--> RESTOK "SET"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 18;  //--> RESTOK "SUBSET"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 19;  //--> RESTOK "ACCESS"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 20;  //--> RESTOK "REMAPS"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 21;  //--> RESTOK "DATABASE"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 22;  //--> RESTOK "DATA-BASE"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 23;  //--> RESTOK "VIRTUAL"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 24;  //--> RESTOK "OF"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 25;  //--> CHRTOK "("
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 26;  //--> RESTOK "ALPHA"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 27;  //--> RESTOK "NUMBER"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 28;  //--> RESTOK "BOOLEAN"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 29;  //--> RESTOK "REAL"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 30;  //--> RESTOK "FIELD"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 31;  //--> RESTOK "GROUP"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 32;  //--> RESTOK "POPULATION"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 33;  //--> RESTOK "COUNT"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 34;  //--> RESTOK "COMPACT"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 35;  //--> RESTOK "DIRECT"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 36;  //--> RESTOK "ORDERED"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 37;  //--> RESTOK "RANDOM"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 38;  //--> RESTOK "RESTART"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 39;  //--> RESTOK "STANDARD"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 40;  //--> RESTOK "UNORDERED"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 41;  //--> RESTOK "REQUIRED"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 42;  //--> RESTOK "READONLY"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 43;  //--> RESTOK "NULL"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 44;  //--> RESTOK "INITIALVALUE"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 45;  //--> RESTOK "OCCURS"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 46;  //--> RESTOK "OC"
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 47;  //--> CHRTOK ","
  if ((INVOKERESULT = getStructureBody()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<STRUCTURE BODY> EXPECTED");
      ENTITYRESULT = BADENTITY;
    }
    INXESCAPELABEL = ESCAPE_PROCEDURE(ESCSTACK, IESCSTACK);
    if (IESCSTACK <= INITIAL_IESCSTACK) return BADENTITY;
    IESCSTACK = INITIAL_IESCSTACK;
    switch (INXESCAPELABEL) {
      case 17: //--> RESTOK "SET"
      case 18: //--> RESTOK "SUBSET"
      case 19: //--> RESTOK "ACCESS"
      case 20: //--> RESTOK "REMAPS"
      case 21: //--> RESTOK "DATABASE"
      case 22: //--> RESTOK "DATA-BASE"
      case 23: //--> RESTOK "VIRTUAL"
      case 24: //--> RESTOK "OF"
      case 25: //--> CHRTOK "("
      case 26: //--> RESTOK "ALPHA"
      case 27: //--> RESTOK "NUMBER"
      case 28: //--> RESTOK "BOOLEAN"
      case 29: //--> RESTOK "REAL"
      case 30: //--> RESTOK "FIELD"
      case 31: //--> RESTOK "GROUP"
      case 32: //--> RESTOK "POPULATION"
      case 33: //--> RESTOK "COUNT"
      case 34: //--> RESTOK "COMPACT"
      case 35: //--> RESTOK "DIRECT"
      case 36: //--> RESTOK "ORDERED"
      case 37: //--> RESTOK "RANDOM"
      case 38: //--> RESTOK "RESTART"
      case 39: //--> RESTOK "STANDARD"
      case 40: //--> RESTOK "UNORDERED"
      case 41: //--> RESTOK "REQUIRED"
      case 42: //--> RESTOK "READONLY"
      case 43: //--> RESTOK "NULL"
      case 44: //--> RESTOK "INITIALVALUE"
      case 45: //--> RESTOK "OCCURS"
      case 46: //--> RESTOK "OC"
      case 47: goto LABA3; //--> CHRTOK ","
    }
    return BADENTITY;
  } else {
    IESCSTACK = INITIAL_IESCSTACK;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getStructureDeclaration

int getCodeFileName() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "ACCESSROUTINES")))
    goto LABA1B1;
  entityEntered = true;
  model.beginCodeFile(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "DMCONTROL")))
    goto LABA1C1;
  entityEntered = true;
  model.beginCodeFile(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "DMUTILITY")))
    goto LABA1D1;
  entityEntered = true;
  model.beginCodeFile(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if (!(token == Token(RESTOK, "DMSUPPORT")))
    goto LABA1E1;
  entityEntered = true;
  model.beginCodeFile(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1E1:
  if (!(token == Token(RESTOK, "LOBUTILITY")))
    goto LABA1F1;
  entityEntered = true;
  model.beginCodeFile(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1F1:
  if (!(token == Token(RESTOK, "RECOVERY")))
    goto LABA1G1;
  entityEntered = true;
  model.beginCodeFile(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1G1:
  if (!(token == Token(RESTOK, "DATARECOVERY")))
    goto LABA1H1;
  entityEntered = true;
  model.beginCodeFile(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1H1:
  if (!(token == Token(RESTOK, "RECONSTRUCT")))
    goto LABA1I1;
  entityEntered = true;
  model.beginCodeFile(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1I1:
  if (!(token == Token(RESTOK, "REORGANIZATION"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("ACCESSROUTINES");
    SEND("DMCONTROL");
    SEND("DMUTILITY");
    SEND("DMSUPPORT");
    SEND("LOBUTILITY");
    SEND("RECOVERY");
    SEND("DATARECOVERY");
    SEND("RECONSTRUCT");
    SEND("REORGANIZATION");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginCodeFile(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getCodeFileName

int getFileTitle() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getUsercode()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA2;
    }
    return BADENTITY;
  }
  entityEntered = true;
LABA2:
  if ((INVOKERESULT = getFileName()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<FILE NAME> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
LABA3:
  if ((INVOKERESULT = getOnFamily()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getFileTitle

int getUsercode() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
  if (!(token == Token(CHRTOK, "("))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
  if (!(token.type == IDNTOK || token.type == RESTOK)) {
    ERROR("<IDENTIFIER> OR <RESERVED WORD> EXPECTED");
    return BADENTITY;
  }
  model.setUsercode(token.value);
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getUsercode

int getFileName() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
LABA2:
  if (!(token.type == IDNTOK || token.type == RESTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("<IDENTIFIER> OR <RESERVED WORD> EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.addTitleNode(token.value);
  token = parser->nextToken();
LABA3:
  if (!(token == Token(CHRTOK, "/")))
    goto EXIT;
  token = parser->nextToken();
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getFileName

int getOnFamily() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "ON"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ON EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
  if (!(token.type == IDNTOK || token.type == RESTOK)) {
    ERROR("<IDENTIFIER> OR <RESERVED WORD> EXPECTED");
    return BADENTITY;
  }
  model.setFamily(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getOnFamily

int getPhysicalOptions() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(CHRTOK, "("))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.openPhysicalOptions();
  token = parser->nextToken();
  if ((INVOKERESULT = getAttributeList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<ATTRIBUTE LIST> EXPECTED");
    }
    return BADENTITY;
  }
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  model.closePhysicalOptions();
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getPhysicalOptions

int getDefaultEntryList() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if ((INVOKERESULT = getDefaultEntry()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA2A1;
    return BADENTITY;
  }
  entityEntered = true;
LABA3:
  if (!(token == Token(CHRTOK, ",")))
    goto LABA4;
  token = parser->nextToken();
LABA4:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getDefaultEntryList

int getDefaultEntry() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if ((INVOKERESULT = getStructureDefault()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1B1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1B1:
  if ((INVOKERESULT = getAttribute()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("<STRUCTURE DEFAULT>");
      SEND("<ATTRIBUTE>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getDefaultEntry

int getStructureDefault() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getStructureDefaultKind()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<STRUCTURE DEFAULT KIND> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
  if ((INVOKERESULT = getPhysicalOptions()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<PHYSICAL OPTIONS> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getStructureDefault

int getAttribute() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token.type == IDNTOK || token.type == RESTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("<IDENTIFIER> OR <RESERVED WORD> EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginAttribute(token.value, token.line);
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getAttributeValue()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getAttribute

int getStructureDefaultKind() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
  goto LABA1B1;
LABA1A1:
  if ((INVOKERESULT = getDataSetWord()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("SUBSET");
      SEND("<DATA SET WORD>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "SET")))
    goto LABA1C1;
  entityEntered = true;
  model.noteDefaultKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "SUBSET")))
    goto LABA1D1;
  entityEntered = true;
  model.noteDefaultKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if (!(token == Token(RESTOK, "ACCESS")))
    goto LABA1E1;
  entityEntered = true;
  model.noteDefaultKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1E1:
  if (!(token == Token(RESTOK, "ALPHA")))
    goto LABA1F1;
  entityEntered = true;
  model.noteDefaultKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1F1:
  if (!(token == Token(RESTOK, "NUMBER")))
    goto LABA1G1;
  entityEntered = true;
  model.noteDefaultKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1G1:
  if (!(token == Token(RESTOK, "REAL")))
    goto LABA1H1;
  entityEntered = true;
  model.noteDefaultKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1H1:
  if (!(token == Token(RESTOK, "FIELD")))
    goto LABA1A1;
  entityEntered = true;
  model.noteDefaultKind(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getStructureDefaultKind

int getDataSetWord() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "DATA")))
    goto LABA1B1;
  entityEntered = true;
  model.noteDataSetWord();
  token = parser->nextToken();
LABA1A2:
  if (!(token == Token(RESTOK, "SET")))
    goto EXIT;
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "DATA-SET")))
    goto LABA1C1;
  entityEntered = true;
  model.noteDataSetWord();
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "DATASET"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("DATA");
    SEND("DATA-SET");
    SEND("DATASET");
    return BADENTITY;
  }
  entityEntered = true;
  model.noteDataSetWord();
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getDataSetWord

int getStructureBody() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
  goto LABA1G1;
LABA1A1:
  if ((INVOKERESULT = getSetDeclaration()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1B1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1B1:
  if ((INVOKERESULT = getSubsetDeclaration()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1C1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1C1:
  if ((INVOKERESULT = getAccessDeclaration()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1D1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1D1:
  if ((INVOKERESULT = getRemapDeclaration()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1E1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1E1:
  if ((INVOKERESULT = getLogicalDatabaseDeclaration()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1F1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1F1:
  if ((INVOKERESULT = getVirtualDeclaration()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1H1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1G1:
  if (!(token == Token(RESTOK, "OF")))
    goto LABA1A1;
  entityEntered = true;
  model.setKind("of");
  token = parser->nextToken();
  if (!(token.type == IDNTOK)) {
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  model.setTarget(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1H1:
  if ((INVOKERESULT = getPhysicalOptions()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1I1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1I1:
  if ((INVOKERESULT = getItemDescription()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1J1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1J1:
  if ((INVOKERESULT = getItemAttributeList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("OF");
      SEND("<SET DECLARATION>");
      SEND("<SUBSET DECLARATION>");
      SEND("<ACCESS DECLARATION>");
      SEND("<REMAP DECLARATION>");
      SEND("<LOGICAL DATABASE DECLARATION>");
      SEND("<VIRTUAL DECLARATION>");
      SEND("<PHYSICAL OPTIONS>");
      SEND("<ITEM DESCRIPTION>");
      SEND("<ITEM ATTRIBUTE LIST>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getStructureBody

int getSetDeclaration() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "SET"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("SET EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setKind("set");
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "OF"))) {
    ERROR("OF EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if (!(token.type == IDNTOK)) {
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  model.setTarget(token.value);
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getWhereCondition()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA3;
    }
    return BADENTITY;
  }
LABA3:
  if ((INVOKERESULT = getSetType()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA4;
    }
    return BADENTITY;
  }
LABA4:
  if ((INVOKERESULT = getPhysicalOptions()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSetDeclaration

int getSubsetDeclaration() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "SUBSET"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("SUBSET EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setKind("subset");
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "OF"))) {
    ERROR("OF EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if (!(token.type == IDNTOK)) {
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  model.setTarget(token.value);
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getWhereCondition()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA3;
    }
    return BADENTITY;
  }
LABA3:
  if ((INVOKERESULT = getSetType()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA4;
    }
    return BADENTITY;
  }
LABA4:
  if ((INVOKERESULT = getPhysicalOptions()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSubsetDeclaration

int getAccessDeclaration() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "ACCESS"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ACCESS EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setKind("access");
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "TO"))) {
    ERROR("TO EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if (!(token.type == IDNTOK)) {
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  model.setTarget(token.value);
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getWhereCondition()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA3;
    }
    return BADENTITY;
  }
LABA3:
  if ((INVOKERESULT = getSetType()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA4;
    }
    return BADENTITY;
  }
LABA4:
  if ((INVOKERESULT = getPhysicalOptions()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getAccessDeclaration

int getRemapDeclaration() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "REMAPS"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("REMAPS EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setKind("remap");
  token = parser->nextToken();
  if (!(token.type == IDNTOK)) {
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  model.setTarget(token.value);
  token = parser->nextToken();
LABA2:
  if (!(token.type == STRTOK))
    goto LABA3;
  model.setComment(token.value);
  token = parser->nextToken();
LABA3:
  if ((INVOKERESULT = getItemAttributeList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA4;
    }
    return BADENTITY;
  }
LABA4:
  if ((INVOKERESULT = getRemapRecordDescription()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<REMAP RECORD DESCRIPTION> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getRemapDeclaration

int getLogicalDatabaseDeclaration() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getDatabaseWord()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<DATABASE WORD> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
  if (!(token == Token(CHRTOK, "("))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.openScope();
  token = parser->nextToken();
  if ((INVOKERESULT = getStructureList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<STRUCTURE LIST> EXPECTED");
    }
    return BADENTITY;
  }
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  model.closeScope();
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getLogicalDatabaseOption()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getLogicalDatabaseDeclaration

int getVirtualDeclaration() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "VIRTUAL"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("VIRTUAL EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.markVirtual();
  token = parser->nextToken();
LABA2:
  if (!(token.type == STRTOK))
    goto LABA3;
  model.setComment(token.value);
  token = parser->nextToken();
LABA3:
  if ((INVOKERESULT = getItemDescription()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<ITEM DESCRIPTION> EXPECTED");
    }
    return BADENTITY;
  }
LABA4:
  if ((INVOKERESULT = getVirtualValue()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getVirtualDeclaration

int getItemDescription() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if ((INVOKERESULT = getAlphaItem()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1B1;
    return BADENTITY;
  }
  entityEntered = true;
  goto LABA2;
LABA1B1:
  if ((INVOKERESULT = getNumberItem()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1C1;
    return BADENTITY;
  }
  entityEntered = true;
  goto LABA2;
LABA1C1:
  if ((INVOKERESULT = getBooleanItem()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1D1;
    return BADENTITY;
  }
  entityEntered = true;
  goto LABA2;
LABA1D1:
  if ((INVOKERESULT = getRealItem()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1E1;
    return BADENTITY;
  }
  entityEntered = true;
  goto LABA2;
LABA1E1:
  if ((INVOKERESULT = getFieldItem()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1F1;
    return BADENTITY;
  }
  entityEntered = true;
  goto LABA2;
LABA1F1:
  if ((INVOKERESULT = getGroupItem()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1G1;
    return BADENTITY;
  }
  entityEntered = true;
  goto LABA2;
LABA1G1:
  if ((INVOKERESULT = getPopulationItem()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1H1;
    return BADENTITY;
  }
  entityEntered = true;
  goto LABA2;
LABA1H1:
  if ((INVOKERESULT = getCountItem()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1I1;
    return BADENTITY;
  }
  entityEntered = true;
  goto LABA2;
LABA1I1:
  if ((INVOKERESULT = getDataSetDeclaration()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("<ALPHA ITEM>");
      SEND("<NUMBER ITEM>");
      SEND("<BOOLEAN ITEM>");
      SEND("<REAL ITEM>");
      SEND("<FIELD ITEM>");
      SEND("<GROUP ITEM>");
      SEND("<POPULATION ITEM>");
      SEND("<COUNT ITEM>");
      SEND("<DATA SET DECLARATION>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
LABA2:
  if ((INVOKERESULT = getItemAttributeList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getItemDescription

int getItemAttributeList() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if ((INVOKERESULT = getItemAttribute()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA2A1;
    return BADENTITY;
  }
  entityEntered = true;
LABA3:
  if (!(token == Token(CHRTOK, ",")))
    goto LABA4;
  token = parser->nextToken();
LABA4:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getItemAttributeList

int getDataSetDeclaration() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getDataSetKind()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA2;
    }
    return BADENTITY;
  }
  entityEntered = true;
LABA2:
  if ((INVOKERESULT = getDataSetWord()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<DATA SET WORD> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
  if ((INVOKERESULT = getRecordDescription()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<RECORD DESCRIPTION> EXPECTED");
    }
    return BADENTITY;
  }
LABA3:
  if ((INVOKERESULT = getStructureTail()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getDataSetDeclaration

int getDataSetKind() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "COMPACT")))
    goto LABA1B1;
  entityEntered = true;
  model.setDatasetKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "DIRECT")))
    goto LABA1C1;
  entityEntered = true;
  model.setDatasetKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "ORDERED")))
    goto LABA1D1;
  entityEntered = true;
  model.setDatasetKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if (!(token == Token(RESTOK, "RANDOM")))
    goto LABA1E1;
  entityEntered = true;
  model.setDatasetKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1E1:
  if (!(token == Token(RESTOK, "RESTART")))
    goto LABA1F1;
  entityEntered = true;
  model.setDatasetKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1F1:
  if (!(token == Token(RESTOK, "STANDARD")))
    goto LABA1G1;
  entityEntered = true;
  model.setDatasetKind(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1G1:
  if (!(token == Token(RESTOK, "UNORDERED"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("COMPACT");
    SEND("DIRECT");
    SEND("ORDERED");
    SEND("RANDOM");
    SEND("RESTART");
    SEND("STANDARD");
    SEND("UNORDERED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setDatasetKind(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getDataSetKind

int getRecordDescription() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(CHRTOK, "("))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.openScope();
  token = parser->nextToken();
  if ((INVOKERESULT = getRecordEntryList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<RECORD ENTRY LIST> EXPECTED");
    }
    return BADENTITY;
  }
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  model.closeScope();
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getRecordDescription

int getStructureTail() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
  goto LABA1B1;
LABA1A1:
  if ((INVOKERESULT = getPhysicalOptions()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND(",");
      SEND("<PHYSICAL OPTIONS>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1B1:
  if (!(token == Token(CHRTOK, ",")))
    goto LABA1A1;
  entityEntered = true;
  token = parser->nextToken();
LABA1B2:
  if ((INVOKERESULT = getAttributeList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<ATTRIBUTE LIST> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getStructureTail

int getAttributeList() {
  bool entityEntered = false;
  int INITIAL_IESCSTACK = 0;
  int INXESCAPELABEL = 0;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  INITIAL_IESCSTACK = IESCSTACK;
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 50;  //--> CHRTOK ","
  if ((INVOKERESULT = getAttribute()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      IESCSTACK = INITIAL_IESCSTACK;
      goto LABA2A1;
    }
    INXESCAPELABEL = ESCAPE_PROCEDURE(ESCSTACK, IESCSTACK);
    if (IESCSTACK <= INITIAL_IESCSTACK) return BADENTITY;
    IESCSTACK = INITIAL_IESCSTACK;
    if (INXESCAPELABEL == 50) goto LABA3; //--> CHRTOK ","
    return BADENTITY;
  } else {
    IESCSTACK = INITIAL_IESCSTACK;
  }
  entityEntered = true;
LABA3:
  if (!(token == Token(CHRTOK, ",")))
    goto LABA4;
  token = parser->nextToken();
LABA4:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getAttributeList

int getRecordEntryList() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if ((INVOKERESULT = getRecordEntry()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA2A1;
    return BADENTITY;
  }
  entityEntered = true;
LABA2B2:
  if (!(token == Token(CHRTOK, ";")))
    goto LABA3;
  token = parser->nextToken();
LABA3:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getRecordEntryList

int getRecordEntry() {
  bool entityEntered = false;
  int INITIAL_IESCSTACK = 0;
  int INXESCAPELABEL = 0;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "FILLER")))
    goto LABA1B1;
  entityEntered = true;
  model.beginFiller(token.line);
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "SIZE")))
    goto LABA1B1;
  token = parser->nextToken();
  if (!(token.type == INTTOK))
    goto LABA1B1;
  model.setFillerSize(token.intValue);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token.type == IDNTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("FILLER");
    SEND("<IDENTIFIER>");
    ENTITYRESULT = BADENTITY;
    INITIAL_IESCSTACK = IESCSTACK;
    ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 48;  //--> RESTOK "FILLER"
    INXESCAPELABEL = ESCAPE_PROCEDURE(ESCSTACK, IESCSTACK);
    if (IESCSTACK <= INITIAL_IESCSTACK) return BADENTITY;
    IESCSTACK = INITIAL_IESCSTACK;
    if (INXESCAPELABEL == 48) goto LABA1A1; //--> RESTOK "FILLER"
    return BADENTITY;
  }
  entityEntered = true;
  model.beginEntry(token.value, token.line);
  token = parser->nextToken();
LABA1B2:
  INITIAL_IESCSTACK = IESCSTACK;
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 48;  //--> RESTOK "FILLER"
  if ((INVOKERESULT = getRename()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      IESCSTACK = INITIAL_IESCSTACK;
      goto LABA1B3;
    }
    INXESCAPELABEL = ESCAPE_PROCEDURE(ESCSTACK, IESCSTACK);
    if (IESCSTACK <= INITIAL_IESCSTACK) return BADENTITY;
    IESCSTACK = INITIAL_IESCSTACK;
    if (INXESCAPELABEL == 48) goto LABA1A1; //--> RESTOK "FILLER"
    return BADENTITY;
  } else {
    IESCSTACK = INITIAL_IESCSTACK;
  }
LABA1B3:
  if (!(token.type == STRTOK))
    goto LABA1B4;
  model.setComment(token.value);
  token = parser->nextToken();
LABA1B4:
  INITIAL_IESCSTACK = IESCSTACK;
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 48;  //--> RESTOK "FILLER"
  if ((INVOKERESULT = getStructureBody()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      IESCSTACK = INITIAL_IESCSTACK;
      goto EXIT;
    }
    INXESCAPELABEL = ESCAPE_PROCEDURE(ESCSTACK, IESCSTACK);
    if (IESCSTACK <= INITIAL_IESCSTACK) return BADENTITY;
    IESCSTACK = INITIAL_IESCSTACK;
    if (INXESCAPELABEL == 48) goto LABA1A1; //--> RESTOK "FILLER"
    return BADENTITY;
  } else {
    IESCSTACK = INITIAL_IESCSTACK;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getRecordEntry

int getRename() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
  if (!(token == Token(CHRTOK, "="))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("= EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
  if (!(token.type == IDNTOK)) {
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  model.setRename(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getRename

int getAlphaItem() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "ALPHA"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ALPHA EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setItemType("alpha");
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, "("))) {
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if (!(token.type == INTTOK)) {
    ERROR("<INTEGER> EXPECTED");
    return BADENTITY;
  }
  model.setSize(token.intValue);
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getAlphaItem

int getNumberItem() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "NUMBER"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("NUMBER EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setItemType("number");
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getNumericSize()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getNumberItem

int getBooleanItem() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "BOOLEAN"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("BOOLEAN EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setItemType("boolean");
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getBooleanItem

int getRealItem() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "REAL"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("REAL EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setItemType("real");
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getNumericSize()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getRealItem

int getFieldItem() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "FIELD"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("FIELD EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setItemType("field");
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, "("))) {
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  model.openScope();
  token = parser->nextToken();
  if ((INVOKERESULT = getFieldBody()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<FIELD BODY> EXPECTED");
    }
    return BADENTITY;
  }
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  model.closeScope();
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getFieldItem

int getGroupItem() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "GROUP"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("GROUP EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setItemType("group");
  token = parser->nextToken();
  if ((INVOKERESULT = getRecordDescription()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<RECORD DESCRIPTION> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getGroupItem

int getPopulationItem() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "POPULATION"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("POPULATION EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setItemType("population");
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, "("))) {
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if (!(token.type == INTTOK)) {
    ERROR("<INTEGER> EXPECTED");
    return BADENTITY;
  }
  model.setSize(token.intValue);
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getOfStructure()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getPopulationItem

int getCountItem() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "COUNT"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("COUNT EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.setItemType("count");
  token = parser->nextToken();
  goto LABA2B1;
LABA2A1:
  if ((INVOKERESULT = getOfStructure()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("(");
      SEND("<OF STRUCTURE>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  goto EXIT;
LABA2B1:
  if (!(token == Token(CHRTOK, "(")))
    goto LABA2A1;
  token = parser->nextToken();
  if (!(token.type == INTTOK)) {
    ERROR("<INTEGER> EXPECTED");
    return BADENTITY;
  }
  model.setSize(token.intValue);
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getCountItem

int getNumericSize() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(CHRTOK, "("))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
LABA2A1:
  if (!(token.type == INTTOK))
    goto LABA2B1;
  model.setSize(token.intValue);
  token = parser->nextToken();
  goto LABA3;
LABA2B1:
  if (!(token.type == IDNTOK)) {
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("<INTEGER>");
    SEND("<IDENTIFIER>");
    return BADENTITY;
  }
  model.setSignedSize(token.value);
  token = parser->nextToken();
LABA3:
  if ((INVOKERESULT = getScaleFactor()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA4;
    }
    return BADENTITY;
  }
LABA4:
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getNumericSize

int getScaleFactor() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
  if (!(token == Token(CHRTOK, ","))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR(", EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
  if (!(token.type == INTTOK)) {
    ERROR("<INTEGER> EXPECTED");
    return BADENTITY;
  }
  model.setScale(token.intValue);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getScaleFactor

int getFieldBody() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if (!(token.type == INTTOK))
    goto LABA1B1;
  entityEntered = true;
  model.setSize(token.intValue);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if ((INVOKERESULT = getRecordEntryList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("<INTEGER>");
      SEND("<RECORD ENTRY LIST>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getFieldBody

int getOfStructure() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "OF"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("OF EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
  if (!(token.type == IDNTOK)) {
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  model.setTarget(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getOfStructure

int getItemAttribute() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "REQUIRED")))
    goto LABA1B1;
  entityEntered = true;
  model.markRequired();
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "READONLY")))
    goto LABA1C1;
  entityEntered = true;
  model.markReadOnly();
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "NULL")))
    goto LABA1D1;
  entityEntered = true;
  model.expectNullValue();
  token = parser->nextToken();
LABA1C2:
  if ((INVOKERESULT = getIsOrEquals()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA1C3;
    }
    return BADENTITY;
  }
LABA1C3:
  if ((INVOKERESULT = getItemValue()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<ITEM VALUE> EXPECTED");
    }
    return BADENTITY;
  }
  goto EXIT;
LABA1D1:
  if (!(token == Token(RESTOK, "INITIALVALUE")))
    goto LABA1E1;
  entityEntered = true;
  model.expectInitialValue();
  token = parser->nextToken();
LABA1D2:
  if ((INVOKERESULT = getIsOrEquals()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA1D3;
    }
    return BADENTITY;
  }
LABA1D3:
  if ((INVOKERESULT = getItemValue()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1E1;
    return BADENTITY;
  }
  goto EXIT;
LABA1E1:
  if ((INVOKERESULT = getOccursClause()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1F1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1F1:
  if ((INVOKERESULT = getAttribute()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("REQUIRED");
      SEND("READONLY");
      SEND("NULL");
      SEND("INITIALVALUE");
      SEND("<OCCURS CLAUSE>");
      SEND("<ATTRIBUTE>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getItemAttribute

int getIsOrEquals() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "IS")))
    goto LABA1B1;
  entityEntered = true;
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(CHRTOK, "="))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("IS");
    SEND("=");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getIsOrEquals

int getItemValue() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "HIGH-VALUE")))
    goto LABA1B1;
  entityEntered = true;
  model.setPendingValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "LOW-VALUE")))
    goto LABA1C1;
  entityEntered = true;
  model.setPendingValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "BLANKS")))
    goto LABA1D1;
  entityEntered = true;
  model.setPendingValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if (!(token == Token(RESTOK, "TRUE")))
    goto LABA1E1;
  entityEntered = true;
  model.setPendingValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1E1:
  if (!(token == Token(RESTOK, "FALSE")))
    goto LABA1F1;
  entityEntered = true;
  model.setPendingValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1F1:
  if (!(token.type == INTTOK))
    goto LABA1G1;
  entityEntered = true;
  model.setPendingValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1G1:
  if (!(token.type == NUMTOK))
    goto LABA1H1;
  entityEntered = true;
  model.setPendingValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1H1:
  if (!(token.type == STRTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("HIGH-VALUE");
    SEND("LOW-VALUE");
    SEND("BLANKS");
    SEND("TRUE");
    SEND("FALSE");
    SEND("<INTEGER>");
    SEND("<NUMBER>");
    SEND("<STRING>");
    return BADENTITY;
  }
  entityEntered = true;
  model.setPendingValue(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getItemValue

int getOccursClause() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "OCCURS") || token == Token(RESTOK, "OC"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("OCCURS OR OC EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
  if (!(token.type == INTTOK)) {
    ERROR("<INTEGER> EXPECTED");
    return BADENTITY;
  }
  model.setOccurs(token.intValue);
  token = parser->nextToken();
LABA2:
  if (!(token == Token(RESTOK, "TIMES")))
    goto LABA3;
  token = parser->nextToken();
LABA3:
  if ((INVOKERESULT = getDependingOn()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getOccursClause

int getDependingOn() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "DEPENDING"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("DEPENDING EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
LABA2:
  if (!(token == Token(RESTOK, "ON")))
    goto LABA3;
  token = parser->nextToken();
LABA3:
  if (!(token.type == IDNTOK)) {
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  model.setDepending(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getDependingOn

int getWhereCondition() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "WHERE"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("WHERE EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginWhere();
  token = parser->nextToken();
  if ((INVOKERESULT = getBooleanExpression()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<BOOLEAN EXPRESSION> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getWhereCondition

int getSetType() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "BIT")))
    goto LABA1B1;
  entityEntered = true;
  model.setSetType("BIT VECTOR");
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "VECTOR")))
    goto LABA1B1;
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "LIST")))
    goto LABA1C1;
  entityEntered = true;
  model.setSetType("LIST");
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "UNORDERED")))
    goto LABA1D1;
  entityEntered = true;
  model.setSetType("UNORDERED LIST");
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "LIST"))) {
    ERROR("LIST EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if ((INVOKERESULT = getKeyClause()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("BIT");
      SEND("LIST");
      SEND("UNORDERED");
      SEND("<KEY CLAUSE>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSetType

int getKeyClause() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "KEY"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("KEY EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
LABA2:
  if (!(token == Token(RESTOK, "IS")))
    goto LABA3;
  token = parser->nextToken();
LABA3:
  if ((INVOKERESULT = getKeySpec()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<KEY SPEC> EXPECTED");
    }
    return BADENTITY;
  }
LABA4:
  if ((INVOKERESULT = getSetTail()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getKeyClause

int getKeySpec() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if (!(token.type == IDNTOK))
    goto LABA1B1;
  entityEntered = true;
  model.addKey(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(CHRTOK, "("))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("<IDENTIFIER>");
    SEND("(");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
  if ((INVOKERESULT = getKeyItemList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<KEY ITEM LIST> EXPECTED");
    }
    return BADENTITY;
  }
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getKeySpec

int getSetTail() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(CHRTOK, ",")))
    goto LABA2;
  entityEntered = true;
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getSetOptionList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<SET OPTION LIST> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSetTail

int getSetOptionList() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if ((INVOKERESULT = getSetOption()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA2A1;
    return BADENTITY;
  }
  entityEntered = true;
LABA3:
  if (!(token == Token(CHRTOK, ",")))
    goto LABA4;
  token = parser->nextToken();
LABA4:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSetOptionList

int getSetOption() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "DUPLICATES")))
    goto LABA1B1;
  entityEntered = true;
  model.setDuplicates(true);
  token = parser->nextToken();
LABA1A2:
  if ((INVOKERESULT = getDuplicatesOption()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "NO")))
    goto LABA1C1;
  entityEntered = true;
  model.noteNo();
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "DUPLICATES"))) {
    ERROR("DUPLICATES EXPECTED");
    return BADENTITY;
  }
  model.noteDuplicates();
  token = parser->nextToken();
LABA1B2:
  if ((INVOKERESULT = getDuplicatesOption()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "INDEX")))
    goto LABA1D1;
  entityEntered = true;
  model.setSetType("INDEX");
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "RANDOM") || token == Token(RESTOK, "SEQUENTIAL"))) {
    ERROR("RANDOM OR SEQUENTIAL EXPECTED");
    return BADENTITY;
  }
  model.setSetType("INDEX " + dasdl_model::toUpper(token.value));
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if (!(token == Token(RESTOK, "ORDERED")))
    goto LABA1E1;
  entityEntered = true;
  model.setSetType("ORDERED LIST");
  token = parser->nextToken();
  if (!(token == Token(RESTOK, "LIST")))
    goto LABA1E1;
  token = parser->nextToken();
  goto EXIT;
LABA1E1:
  if (!(token == Token(RESTOK, "I-R")))
    goto LABA1F1;
  entityEntered = true;
  model.setSetType("INDEX RANDOM");
  token = parser->nextToken();
  goto EXIT;
LABA1F1:
  if (!(token == Token(RESTOK, "I-S")))
    goto LABA1G1;
  entityEntered = true;
  model.setSetType("INDEX SEQUENTIAL");
  token = parser->nextToken();
  goto EXIT;
LABA1G1:
  if (!(token == Token(RESTOK, "O-L")))
    goto LABA1H1;
  entityEntered = true;
  model.setSetType("ORDERED LIST");
  token = parser->nextToken();
  goto EXIT;
LABA1H1:
  if (!(token == Token(RESTOK, "DATA")))
    goto LABA1I1;
  entityEntered = true;
  model.beginDataItems();
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, "("))) {
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if ((INVOKERESULT = getKeyItemList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<KEY ITEM LIST> EXPECTED");
    }
    return BADENTITY;
  }
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  model.endDataItems();
  token = parser->nextToken();
  goto EXIT;
LABA1I1:
  if ((INVOKERESULT = getAttribute()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("DUPLICATES");
      SEND("NO");
      SEND("INDEX");
      SEND("ORDERED");
      SEND("I-R");
      SEND("I-S");
      SEND("O-L");
      SEND("DATA");
      SEND("<ATTRIBUTE>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSetOption

int getDuplicatesOption() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "FIRST")))
    goto LABA1B1;
  entityEntered = true;
  model.setDuplicatesOption(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "LAST")))
    goto LABA1C1;
  entityEntered = true;
  model.setDuplicatesOption(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "KEYCHANGEOK"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("FIRST");
    SEND("LAST");
    SEND("KEYCHANGEOK");
    return BADENTITY;
  }
  entityEntered = true;
  model.setDuplicatesOption(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getDuplicatesOption

int getKeyItemList() {
  bool entityEntered = false;
  int INITIAL_IESCSTACK = 0;
  int INXESCAPELABEL = 0;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
LABA2:
  INITIAL_IESCSTACK = IESCSTACK;
  ESCSTACK[static_cast<size_t>(++IESCSTACK)] = 49;  //--> CHRTOK ","
  if ((INVOKERESULT = getKeyItem()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered) {
        IESCSTACK = INITIAL_IESCSTACK;
        return ISNTENTITY;
      }
      ERROR("<KEY ITEM> EXPECTED");
      ENTITYRESULT = BADENTITY;
    }
    INXESCAPELABEL = ESCAPE_PROCEDURE(ESCSTACK, IESCSTACK);
    if (IESCSTACK <= INITIAL_IESCSTACK) return BADENTITY;
    IESCSTACK = INITIAL_IESCSTACK;
    if (INXESCAPELABEL == 49) goto LABA3; //--> CHRTOK ","
    return BADENTITY;
  } else {
    IESCSTACK = INITIAL_IESCSTACK;
  }
  entityEntered = true;
LABA3:
  if (!(token == Token(CHRTOK, ",")))
    goto EXIT;
  token = parser->nextToken();
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getKeyItemList

int getKeyItem() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token.type == IDNTOK || token.type == RESTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("<IDENTIFIER> OR <RESERVED WORD> EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.addKey(token.value);
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getKeyOrder()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getKeyItem

int getKeyOrder() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "ASCENDING")))
    goto LABA1B1;
  entityEntered = true;
  model.setKeyOrder(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "DESCENDING"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("ASCENDING");
    SEND("DESCENDING");
    return BADENTITY;
  }
  entityEntered = true;
  model.setKeyOrder(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getKeyOrder

int getAttributeValue() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(CHRTOK, "="))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("= EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  token = parser->nextToken();
  if ((INVOKERESULT = getValueExpression()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<VALUE EXPRESSION> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getAttributeValue

int getValueExpression() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if (!(token.type == IDNTOK || token.type == RESTOK))
    goto LABA1B1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token.type == STRTOK))
    goto LABA1C1;
  entityEntered = true;
  model.addAttributeString(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if ((INVOKERESULT = getNumericValue()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("<IDENTIFIER>");
      SEND("<RESERVED WORD>");
      SEND("<STRING>");
      SEND("<NUMERIC VALUE>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getValueExpression

int getNumericValue() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token.type == INTTOK || token.type == NUMTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("<INTEGER> OR <NUMBER> EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getNumericQualifierList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getNumericValue

int getNumericQualifierList() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if ((INVOKERESULT = getNumericQualifier()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA2A1;
    return BADENTITY;
  }
  entityEntered = true;
LABA3:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getNumericQualifierList

int getNumericQualifier() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
  goto LABA1C1;
LABA1A1:
  if ((INVOKERESULT = getUnit()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA1B1;
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1B1:
  if ((INVOKERESULT = getPerClause()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<PER CLAUSE> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
  goto EXIT;
LABA1C1:
  if (!(token == Token(CHRTOK, "+")))
    goto LABA1D1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  if (!(token.type == INTTOK || token.type == NUMTOK)) {
    ERROR("<INTEGER> OR <NUMBER> EXPECTED");
    return BADENTITY;
  }
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if (!(token == Token(RESTOK, "OR")))
    goto LABA1E1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  if (!(token.type == INTTOK || token.type == NUMTOK)) {
    ERROR("<INTEGER> OR <NUMBER> EXPECTED");
    return BADENTITY;
  }
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1E1:
  if (!(token == Token(CHRTOK, "(")))
    goto LABA1A1;
  entityEntered = true;
  token = parser->nextToken();
  if (!(token.type == IDNTOK || token.type == RESTOK)) {
    ERROR("<IDENTIFIER> OR <RESERVED WORD> EXPECTED");
    return BADENTITY;
  }
  model.addAttributeValue("(" + token.value + ")");
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getNumericQualifier

int getUnit() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "WORDS")))
    goto LABA1B1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "BLOCKS")))
    goto LABA1C1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "RECORDS")))
    goto LABA1D1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if (!(token == Token(RESTOK, "ENTRIES")))
    goto LABA1E1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1E1:
  if (!(token == Token(RESTOK, "SEGMENTS")))
    goto LABA1F1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1F1:
  if (!(token == Token(RESTOK, "BYTES")))
    goto LABA1G1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1G1:
  if (!(token == Token(RESTOK, "BITS")))
    goto LABA1H1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1H1:
  if (!(token == Token(RESTOK, "SECONDS")))
    goto LABA1I1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1I1:
  if (!(token == Token(RESTOK, "SYNCPOINTS")))
    goto LABA1J1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1J1:
  if (!(token == Token(RESTOK, "TRANSACTIONS"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("WORDS");
    SEND("BLOCKS");
    SEND("RECORDS");
    SEND("ENTRIES");
    SEND("SEGMENTS");
    SEND("BYTES");
    SEND("BITS");
    SEND("SECONDS");
    SEND("SYNCPOINTS");
    SEND("TRANSACTIONS");
    return BADENTITY;
  }
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getUnit

int getPerClause() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "PER"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("PER EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getBufferKind()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA3;
    }
    return BADENTITY;
  }
LABA3:
  if (!(token == Token(RESTOK, "USER"))) {
    ERROR("USER EXPECTED");
    return BADENTITY;
  }
  model.addAttributeValue(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getPerClause

int getBufferKind() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "RANDOM")))
    goto LABA1B1;
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "SERIAL"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("RANDOM");
    SEND("SERIAL");
    return BADENTITY;
  }
  entityEntered = true;
  model.addAttributeValue(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getBufferKind

int getBooleanExpression() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getBooleanTerm()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<BOOLEAN TERM> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
LABA2:
  if ((INVOKERESULT = getBooleanTail()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getBooleanExpression

int getBooleanTerm() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getNegation()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA2;
    }
    return BADENTITY;
  }
  entityEntered = true;
LABA2:
  if ((INVOKERESULT = getBooleanPrimary()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<BOOLEAN PRIMARY> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getBooleanTerm

int getBooleanTail() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if (!(token == Token(RESTOK, "AND") || token == Token(RESTOK, "OR")))
    goto LABA2A1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  if ((INVOKERESULT = getBooleanTerm()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<BOOLEAN TERM> EXPECTED");
    }
    return BADENTITY;
  }
LABA3:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getBooleanTail

int getNegation() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "NOT")))
    goto LABA1B1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(CHRTOK, "^"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("NOT");
    SEND("^");
    return BADENTITY;
  }
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getNegation

int getBooleanPrimary() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "TRUE")))
    goto LABA1B1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "FALSE")))
    goto LABA1C1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(CHRTOK, "(")))
    goto LABA1D1;
  entityEntered = true;
  model.addExprToken("(");
  token = parser->nextToken();
  if ((INVOKERESULT = getBooleanExpression()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<BOOLEAN EXPRESSION> EXPECTED");
    }
    return BADENTITY;
  }
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  model.addExprToken(")");
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if ((INVOKERESULT = getComparison()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("ONE OF THE FOLLOWING EXPECTED");
      SEND("TRUE");
      SEND("FALSE");
      SEND("(");
      SEND("<COMPARISON>");
      return BADENTITY;
    }
    return BADENTITY;
  }
  entityEntered = true;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getBooleanPrimary

int getComparison() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getOperand()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<OPERAND> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
LABA2:
  if ((INVOKERESULT = getComparisonTail()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getComparison

int getOperand() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getOperandPrimary()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<OPERAND PRIMARY> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
LABA2:
  if ((INVOKERESULT = getArithmeticTail()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getOperand

int getComparisonTail() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getRelationalOperator()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<RELATIONAL OPERATOR> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
  if ((INVOKERESULT = getOperand()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<OPERAND> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getComparisonTail

int getRelationalOperator() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(CHRTOK, "=")))
    goto LABA1B1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "EQL")))
    goto LABA1C1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(RESTOK, "NEQ")))
    goto LABA1D1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if (!(token == Token(RESTOK, "LSS")))
    goto LABA1E1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1E1:
  if (!(token == Token(RESTOK, "LEQ")))
    goto LABA1F1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1F1:
  if (!(token == Token(RESTOK, "GTR")))
    goto LABA1G1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1G1:
  if (!(token == Token(RESTOK, "GEQ")))
    goto LABA1H1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1H1:
  if (!(token == Token(CHRTOK, "<")))
    goto LABA1I1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1I1:
  if (!(token == Token(CHRTOK, ">"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("=");
    SEND("EQL");
    SEND("NEQ");
    SEND("LSS");
    SEND("LEQ");
    SEND("GTR");
    SEND("GEQ");
    SEND("<");
    SEND(">");
    return BADENTITY;
  }
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getRelationalOperator

int getOperandPrimary() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token.type == IDNTOK))
    goto LABA1B1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token.type == INTTOK))
    goto LABA1C1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token.type == NUMTOK))
    goto LABA1D1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if (!(token.type == STRTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("<IDENTIFIER>");
    SEND("<INTEGER>");
    SEND("<NUMBER>");
    SEND("<STRING>");
    return BADENTITY;
  }
  entityEntered = true;
  model.addExprString(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getOperandPrimary

int getArithmeticTail() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if ((INVOKERESULT = getArithmeticOperator()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA2A1;
    return BADENTITY;
  }
  entityEntered = true;
  if ((INVOKERESULT = getOperandPrimary()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<OPERAND PRIMARY> EXPECTED");
    }
    return BADENTITY;
  }
LABA3:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getArithmeticTail

int getArithmeticOperator() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(CHRTOK, "+")))
    goto LABA1B1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(CHRTOK, "-")))
    goto LABA1C1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1C1:
  if (!(token == Token(CHRTOK, "*")))
    goto LABA1D1;
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
  goto EXIT;
LABA1D1:
  if (!(token == Token(CHRTOK, "/"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("+");
    SEND("-");
    SEND("*");
    SEND("/");
    return BADENTITY;
  }
  entityEntered = true;
  model.addExprToken(token.value);
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getArithmeticOperator

int getRemapRecordDescription() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if ((INVOKERESULT = getRecordDescription()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      if (!entityEntered)
          return ISNTENTITY;
      ERROR("<RECORD DESCRIPTION> EXPECTED");
    }
    return BADENTITY;
  }
  entityEntered = true;
LABA2:
  if ((INVOKERESULT = getVariableFormatPart()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA3;
    }
    return BADENTITY;
  }
LABA3:
  if ((INVOKERESULT = getSelectVerifyList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getRemapRecordDescription

int getVariableFormatPart() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if ((INVOKERESULT = getVariableFormatRecord()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA2A1;
    return BADENTITY;
  }
  entityEntered = true;
LABA3:
  if (!(token == Token(CHRTOK, ",")))
    goto LABA4;
  token = parser->nextToken();
LABA4:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getVariableFormatPart

int getSelectVerifyList() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if ((INVOKERESULT = getSelectVerify()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA2A1;
    return BADENTITY;
  }
  entityEntered = true;
LABA3:
  if (!(token == Token(CHRTOK, ",")))
    goto LABA4;
  token = parser->nextToken();
LABA4:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSelectVerifyList

int getVirtualValue() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(CHRTOK, "="))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("= EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginVirtual();
  token = parser->nextToken();
  if ((INVOKERESULT = getBooleanExpression()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<BOOLEAN EXPRESSION> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getVirtualValue

int getVariableFormatRecord() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token.type == INTTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("<INTEGER> EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginFormat(token.intValue, token.line);
  token = parser->nextToken();
  if (!(token == Token(CHRTOK, ":"))) {
    ERROR(": EXPECTED");
    return BADENTITY;
  }
  token = parser->nextToken();
  if ((INVOKERESULT = getRecordDescription()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<RECORD DESCRIPTION> EXPECTED");
    }
    return BADENTITY;
  }
LABA2:
  if ((INVOKERESULT = getSelectVerifyList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getVariableFormatRecord

int getSelectVerify() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "SELECT") || token == Token(RESTOK, "VERIFY"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("SELECT OR VERIFY EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginSelectOrVerify(token.value);
  token = parser->nextToken();
  if ((INVOKERESULT = getBooleanExpression()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<BOOLEAN EXPRESSION> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSelectVerify

int getDatabaseWord() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
LABA1A1:
  if (!(token == Token(RESTOK, "DATABASE")))
    goto LABA1B1;
  entityEntered = true;
  model.setKind("logical-database");
  token = parser->nextToken();
  goto EXIT;
LABA1B1:
  if (!(token == Token(RESTOK, "DATA-BASE"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("DATABASE");
    SEND("DATA-BASE");
    return BADENTITY;
  }
  entityEntered = true;
  model.setKind("logical-database");
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getDatabaseWord

int getStructureList() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if ((INVOKERESULT = getStructureListEntry()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA2A1;
    return BADENTITY;
  }
  entityEntered = true;
LABA3:
  if (!(token == Token(CHRTOK, ",")))
    goto LABA4;
  token = parser->nextToken();
LABA4:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getStructureList

int getLogicalDatabaseOption() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(RESTOK, "GUARDFILE"))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("GUARDFILE EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginGuardFile();
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getIsOrEquals()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA3;
    }
    return BADENTITY;
  }
LABA3:
  if ((INVOKERESULT = getFileTitle()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<FILE TITLE> EXPECTED");
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getLogicalDatabaseOption

int getStructureListEntry() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token.type == IDNTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginEntry(token.value, token.line);
  model.setKind("ldb-entry");
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getRename()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto LABA3;
    }
    return BADENTITY;
  }
LABA3:
  if ((INVOKERESULT = getSetPart()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getStructureListEntry

int getSetPart() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token == Token(CHRTOK, "("))) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("( EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.openScope();
  token = parser->nextToken();
LABA2A1:
  if (!(token == Token(RESTOK, "ALL")))
    goto LABA2B1;
  model.setSetsMode("ALL");
  token = parser->nextToken();
  goto LABA3;
LABA2B1:
  if (!(token == Token(RESTOK, "NONE")))
    goto LABA2C1;
  model.setSetsMode("NONE");
  token = parser->nextToken();
  goto LABA3;
LABA2C1:
  if (!(token == Token(RESTOK, "SET") || token == Token(RESTOK, "SETS"))) {
    ERROR("ONE OF THE FOLLOWING EXPECTED");
    SEND("ALL");
    SEND("NONE");
    SEND("SET");
    SEND("SETS");
    return BADENTITY;
  }
  model.setSetsMode("LIST");
  token = parser->nextToken();
  if ((INVOKERESULT = getSetList()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      ERROR("<SET LIST> EXPECTED");
    }
    return BADENTITY;
  }
LABA3:
  if (!(token == Token(CHRTOK, ")"))) {
    ERROR(") EXPECTED");
    return BADENTITY;
  }
  model.closeScope();
  token = parser->nextToken();
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSetPart

int getSetList() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  goto LABA2B1;
LABA2A1:
  goto EXIT;
LABA2B1:
  if ((INVOKERESULT = getSetListEntry()) != 0) {
    if (INVOKERESULT == ISNTENTITY)
        goto LABA2A1;
    return BADENTITY;
  }
  entityEntered = true;
LABA3:
  if (!(token == Token(CHRTOK, ",")))
    goto LABA4;
  token = parser->nextToken();
LABA4:
  goto LABA1;
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSetList

int getSetListEntry() {
  bool entityEntered = false;
  int ENTITYRESULT = 0;
  int INVOKERESULT = 0;
LABA1:
  if (!(token.type == IDNTOK)) {
    if (!entityEntered)
        return ISNTENTITY;
    ERROR("<IDENTIFIER> EXPECTED");
    return BADENTITY;
  }
  entityEntered = true;
  model.beginEntry(token.value, token.line);
  model.setKind("ldb-set");
  token = parser->nextToken();
LABA2:
  if ((INVOKERESULT = getRename()) != 0) {
    if (INVOKERESULT == ISNTENTITY) {
      goto EXIT;
    }
    return BADENTITY;
  }
EXIT:
  if (!entityEntered)
    return ISNTENTITY;
  return ENTITYRESULT;
} // END OF getSetListEntry

int main(int argc, char **argv) {
  if (INITIALIZE(argc, argv))
    return -1;
  assert(parser && "Parser must be initialized before parsing");
  token = parser->nextToken();
  getRoot();
  if (PROCESS())
    return -1;
  return 0;
}
